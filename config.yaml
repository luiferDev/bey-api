app:
  host: "0.0.0.0"
  port: 8080
  mode: "debug"
  static_path: "./cmd/api/static"
  swagger_enabled: true

database:
  host: "localhost"
  port: 5432
  user: "luifer"
  password: "158365402luifer"
  name: "bey_db"
  sslmode: "disable"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m

concurrency:
  worker_pool:
    worker_pool_size: 4
    queue_depth_limit: 100
  rate_limit:
    enabled: false
    requests_per_second: 100
    burst_capacity: 200
    endpoint_limits: {}
